- Grants broad execution permissions with an explicit directive to "do everything you have to do" and "not ask me again and again" for permission; combined with terse approvals (e.g. "--yolo"), this signals strong comfort with autonomous, low-overhead execution without repeated permission-checking or sign-off between steps. Confidence: 0.85
- Wants a written markdown implementation report as a deliverable at milestone boundaries: an explicit request ("make a report md stating whatever changes were made... in detail and the results architecture built") to produce a comprehensive, phase-by-phase changelog plus a description of the final built architecture, test status, and honest next steps — written alongside the implementation docs and grounded in freshly gathered facts (git status, test runs, file inventory) rather than memory. Confidence: 0.6- Prefers using the existing system Python interpreter with pre-installed requirements rather than creating new virtual environments or installing packages. Confidence: 0.95
- Inserts `src` into `sys.path` (via `os.path`-computed paths) in tests and imports modules without a package prefix (e.g. `from encoders.occupancy_encoder import OccupancyEncoder`). Confidence: 0.95
- Includes an `if __name__ == "__main__"` test runner in test files that iterates over `test_*` functions, prints PASS/FAIL lines, and exits with a status code. Confidence: 0.9
- Writes heavy module-level docstrings that cite architecture documents (§ references) and explain design rationale plus normalization conventions. Confidence: 0.9
- Zero-initializes FiLM/AdaLN-style heads to enforce an identity transform at step 0 (gamma→1, beta→0), matching this repo's AdaLN-zero convention. Confidence: 0.9
- Uses `torch.manual_seed` / `torch.Generator().manual_seed(seed)` to keep test helpers deterministic and reproducible. Confidence: 0.85
- Reuses existing shared components (TransformerBlock, get_2d_sincos_pos_embed, Attention) from geometry_encoder rather than reimplementing them. Confidence: 0.9
- Writes round-trip correctness tests (e.g. assemble(factorize(x)) == x), including against real dataset files when present, with explicit float tolerance. Confidence: 0.8
- Asserts exact output tensor shapes in tests (token count, width, batch/sequence dims). Confidence: 0.9
- Represents scalar known/unknown state with explicit binary flags rather than inferring missingness from a numeric sentinel value. Confidence: 0.85
- Runs tests via `python -m pytest <file> -v`. Confidence: 0.85
- Uses `@pytest.mark.skipif` to conditionally skip tests when external resources (e.g., model weights, GPU) are unavailable, with a clear reason string. Confidence: 0.8
- Creates temporary scripts in the repo root for quick iterative validation/debugging: `_smoke_*.py` for training-loop smoke tests, `_debug_*.py` for isolated gradient-flow investigation (e.g. tracing where `torch.autograd.grad` returns None/NaN/Inf). Also creates temporary config variants (e.g. `configs/unified_smoke.yaml`). All are deleted immediately after to keep the working tree clean. Confidence: 0.8
- Prefers `os.path` for filesystem path computation in all Python files (tests, scripts, modules) over pathlib; uses `REPO_ROOT = os.path.dirname(os.path.abspath(__file__))` patterns with correct nesting-level adjustments per file location. Confidence: 0.9
- Uses `torch.autograd.grad(..., allow_unused=True)` (often with `retain_graph=True`) in debug scripts to trace gradient flow through a specific intermediate tensor, printing None/NaN/Inf diagnostics to pinpoint where gradients die. Confidence: 0.75
- Overrides the zero-init convention for regression/prediction heads whose output feeds directly into a frozen downstream model: initializes the final-layer bias to dataset-distribution means rather than zero, because zero outputs collapse the downstream model into a dead-Jacobian region (e.g. ReLU6 saturating at zero input). Confidence: 0.85
- Implements a straight-through estimator (STE) when a frozen downstream module has a zero Jacobian on soft (non-binary) inputs: the forward pass uses binary/hard values (in-distribution for the frozen module) while the backward path carries gradients through the soft/differentiable pathway. Confidence: 0.85
- Freezes frozen-module parameters with `requires_grad_(False)` + `.eval()` but does NOT wrap their forward pass in `torch.no_grad()` when gradient flow through the module's input is needed (e.g. surrogate physics loss); uses `torch.no_grad()` only around pure evaluation/inference. Confidence: 0.8
- Normalizes spectrum errors by per-sample standard deviation (along the spectrum dimension, clamped to a minimum epsilon) when comparing predicted vs. target spectra, to handle the wide dynamic range of spectral amplitudes across samples. Applied consistently in both loss and evaluation functions. Confidence: 0.8
- Uses `todo_write` tool religiously to track multi-step implementation tasks, creating todos with `activeForm`/`status` fields and updating `pending` → `in_progress` → `completed` as work progresses through each phase. Confidence: 0.9
- Implements classifier-free guidance (CFG) as two-pass inference kept outside the model's `forward`: a separate function runs the model twice (once with `goal_mode="real"`, once with `goal_mode="null"`) and combines via `z_null + w * (z_real - z_null)`, using `@torch.no_grad()` for the inference passes. Prefers this separation over embedding conditional branching inside the model's forward method. Confidence: 0.8
- Uses goal dropout (null-token replacement at ~10% probability) for CFG training: a dedicated `goal_dropout()` utility wraps `torch.rand(1, generator=rng)` for reproducibility, randomly replacing the real goal with a null goal so the model learns both conditional and unconditional prediction. Configurable via `guidance_dropout` in config. Confidence: 0.8
- Sources loss coefficients and staged-training ramp schedules from config via `cfg.get("section", {}).get("param", default)` patterns, using linear ramp schedules (e.g. `min(1.0, (step+1) / ramp_steps)`) to gradually introduce new loss terms like physics loss rather than enabling them abruptly. Confidence: 0.75
- Computes a normalized guidance gap diagnostic for Failure Mode 2 detection: `||z_real - z_null|| / std(z_real)`, measuring how much the goal influences predictions. Swept across mask-ratio buckets; a flat or near-zero gap indicates the predictor ignores the spectrum input. Confidence: 0.75
- Runs training-loop smoke tests via a `--no-train` CLI flag (e.g. `python scripts/train/train_unified.py --config configs/unified.yaml --no-train`) to validate the full pipeline (data loading, forward, loss, logging) without executing actual optimization steps. Confidence: 0.65
- Uses a shared `_StubReleasedEncoder` + `_build_model()` test-helper pattern: a minimal stub encoder (frozen, eval mode) swapped into `model.spectrum_path`, with EMA target state-dicts loaded from the real encoder, returned in eval mode. Repeated across test files for components that depend on the full model forward. Confidence: 0.7
- Avoids backgrounding long-running test suites on Windows cmd and avoids piping pytest output through `findstr`: the pipe buffers output (making the suite appear hung for 10+ minutes) and `findstr` returning no matches exits with code 1 (a false failure). Runs pytest in the foreground with direct output instead. Confidence: 0.7
- Runs the complete test suite (`python -m pytest tests/ -q --tb=line`) as a final regression gate at the end of each phase, in addition to targeted per-file runs; when the suite is slow or appears hung, isolates the problem by running subsets of test files in the foreground with `-v`. Confidence: 0.75
- Performs final repository hygiene checks before declaring work complete: `git status --short`, `git diff --check` (whitespace errors), scanning for leftover `_*.py` debug/temp scripts, and verifying no stale dummy checkpoints or test artifacts remain. Confidence: 0.7
- When test assertions reference a wrong API (nonexistent method name, or a result-dict structure different from what the implementation returns), updates the test to match the implemented contract rather than changing working implementation code. Confidence: 0.6
- Wants completion questions answered with an honest, precise status rather than a blanket "yes": verifies any loose ends (e.g. a script created but never run end-to-end) before replying, and explicitly separates "code implemented and tested" from "scientific validation not yet done" (deferred steps like cloud-GPU training and the real acceptance gate), honoring rules like "do not claim success merely because code runs." Confidence: 0.65
- Treats the implementation MD spec documents (e.g. `docs/implementation/unified_jepa/*.md`, `architecture_v5.md`) as the authoritative source of truth for the codebase: expects completed work to be systematically audited phase-by-phase against the corresponding spec, and deviations — config pointing at the wrong artifact, spec-required metadata not stored, silent behavior choices where the spec mandates a check first, hardcoded values where the spec says configurable, duplicated logic that can diverge — to be fixed rather than accepted as "working". Confidence: 0.85
- Avoids duplicated implementations of the same loss/function (DRY): when a module like the physics loss needs the same computation as an existing authoritative function, delegates to it (`from physics.physics_loop import physics_loss`) instead of inlining a second copy that can silently diverge. Confidence: 0.75
- Makes behavioral choices explicit and configurable rather than silent hardcodes: when a spec mandates a sanity check (e.g. "do not silently choose STE without the check"), adds a documented config flag (`physics_use_ste`) with a comment stating the empirical justification, wires it from config through the constructor, and has the gradient test try the default soft path first with the fallback only when gradients are zero. Confidence: 0.7
- When an evaluation gate is spec-defined for a specific stratum (e.g. "the primary gate is the hard stratum — full mask + all scalars unknown"), evaluates the gate on that stratum and reports easy/hard strata separately rather than pooling or using a single default configuration. Confidence: 0.7
- Commits each bug fix as its own separate commit with its own regression test, so every fix is independently revertible and reviewable; explicitly forbids bundling unrelated refactoring into the same commit. Confidence: 0.8
- Works fixes sequentially with a strict per-fix gate: make the code change, add a regression test (one that would actually fail against the pre-fix code), run it locally, and confirm the full suite still passes before moving to the next fix. Confidence: 0.8
- Flags known coverage limitations in test docstrings — e.g. a CUDA-only failure path guarded by `if torch.cuda.is_available()` that is skipped on CPU-only CI — so a passing test is never mistaken for full coverage of the bug class. Confidence: 0.75
- Makes implicit data assumptions explicit with assertions (e.g. asserts h_atom/r_atom are strictly positive in factorize_geometry) and asserts invariants directly on real data rather than only inferring them from round-trip success. Confidence: 0.7
- When sampling random masks/flags with a fixed `torch.Generator`, draws on CPU with the CPU generator and moves the result to the input tensor's device (`.to(device)`), avoiding device-mismatch RuntimeErrors when inputs are CUDA tensors. Confidence: 0.7
- Does not declare a fix complete when the actual failing path could not be exercised in the current environment (e.g. no CUDA to reproduce a CUDA-only bug); explicitly flags the verification limitation and leaves a permanent regression guard for the next environment that can exercise it. Confidence: 0.7
